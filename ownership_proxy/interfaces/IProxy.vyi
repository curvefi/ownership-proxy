event DelegationSet:
    delegate: indexed(address)
    end_ts: uint256
    checker: indexed(address)

event DelegationKilled:
    delegate: indexed(address)

struct DelegationMetadata:
    end_ts: uint256
    checker: address