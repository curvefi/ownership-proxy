[project]
name = "ownership-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "snekmate>=0.1.1",
    "vyper==0.4.3",
]

[tool.uv]
dev-dependencies = [
    "pytest-xdist>=3.7.0",
    "titanoboa==0.2.7",
]
